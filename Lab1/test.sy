#define main fibonacci

int fibonacci() {
    int a, b, i, t, n;
    
    a = 0;
    b = 1;
    i = 1;
    
    //使用getint()函数读取输入
    n = getint();
    
    //使用putint()和putch()函数输出
    putint(a);
    putch(10);  // 10是换行符的ASCII码
    putint(b);
    putch(10);
    
    while (i < n) {
        t = b;
        b = a + b;
        putint(b);
        putch(10);
        a = t;
        i = i + 1;
    }
    
    return 0;
}